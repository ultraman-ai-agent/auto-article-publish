# 项目规则

## 文档维护（强约束）

- 每次**新增功能**或**修改功能**，必须同步维护文档，未更新文档视为任务未完成。
- 文档分为两份，均位于 `docs/`：
  - `docs/业务文档.md`：需求背景、功能清单、使用场景、操作流程、待办事项。
  - `docs/技术文档.md`：架构、模块职责、目录结构、配置项、外部接口、运行与排障。
- 触发边界：
  - 新增功能、改功能行为、改配置项/接口 → 业务文档与技术文档**都必更**。
  - 纯内部重构、不影响行为的 bug 修复 → 技术文档按需更新。
  - 仅改文案/格式 → 无需更新。

## 编码约定

- 语言：Python 3.10+。
- 对话与代码注释使用中文。
- 密钥、AppID、Token 一律走 `.env`，禁止硬编码进源码，禁止提交进 git。
- 图片文件名使用 ASCII 字符（小红书对中文路径兼容性差）。

## 运行前置

- 小红书发布依赖本地 `xiaohongshu-mcp-windows-amd64.exe` 服务（默认 `http://localhost:18060/mcp`）。
- 首次使用需运行 `xiaohongshu-login-windows-amd64.exe` 扫码登录。
